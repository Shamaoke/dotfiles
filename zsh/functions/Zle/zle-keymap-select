# change prompt according to the input mode
case $KEYMAP in
  main)  PS1="%~ %F{10}%B>_%b%f " ;;
  vicmd) PS1="%~ %F{9}%B>:%b%f "
esac

zle reset-prompt

# vim: set filetype=zsh textwidth=80 colorcolumn=80:
