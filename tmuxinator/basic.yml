name: <%= @args[0] || (File.basename Dir.pwd) %>

startup_pane: 1

windows:
  - editor: vim
  - work:
  - build|install:
      layout: even-horizontal
      panes:
        -
        -
  - console:
  - fm: vifm
