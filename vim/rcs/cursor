" устранить задержку при переключении режимов
set ttimeoutlen=0
" для устранения задержки при работе в Tmux'е добавить `set -g escape-time 0' в .tmux.conf

" изменять форму курсора в зависимости от текущего режима (редактирование, замена, обзор)
" посылать специфичные escape-последовательности при работе в Tmux'е
if exists("$TMUX")
  " задать форму курсора при переключении в режим редактирования (Tmux)
  let &t_SI = "\<Esc>Ptmux;\<Esc>\<Esc>[6 q\<Esc>\\"
  " задать форму курсора при переключении в режим замены (Tmux)
  let &t_SR = "\<Esc>Ptmux;\<Esc>\<Esc>[4 q\<Esc>\\"
  " задать форму курсора при выходе из режима редактирования или замены (Tmux)
  let &t_EI = "\<Esc>Ptmux;\<Esc>\<Esc>[2 q\<Esc>\\"
else
  " задать форму курсора при переключении в режим редактирования
  let &t_SI = "\<Esc>[6 q"
  " задать форму курсора при переключении в режим замены
  let &t_SR = "\<Esc>[4 q"
  " задать форму курсора при выходе из режима редактирования или замены
  let &t_EI = "\<Esc>[2 q"
endif
" подробнее см `:help termcap-cursor-shape'

" Перечень значений и соответствующих им форм курсора
" (значения специфичны для urxvt)

" 1 — мерцающий блок
" 2 — блок
" 3 — мерцающее подчеркивание
" 4 — подчеркивание
" 5 — мерцающая вертикальная черта
" 6 — вертикальная черта

" vim: set filetype=vim textwidth=80 colorcolumn=80:
