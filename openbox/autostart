picom &

amixer set Master 95% &
amixer --card 0 set Speaker 100% unmute &

ibus-daemon -drx &

pointer_hide &

touchpad_toggle &

~/.fehbg &

# devilspie &

dunst &

# reset the current sound device to the default one (internal sound card)
# soundreset &

# launch polybar
~/.config/polybar/launch &

# picom --daemon &

# start a jack session through cadence at startup
# cadence-session-start --start &

# vim: set filetype=zsh textwidth=80 colorcolumn=80:
